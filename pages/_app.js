// restart the server when you add this component

// global css rules are imported here

import '../styles/global.css'

export default function App({ Component, pageProps }) {
    return <Component {...pageProps}/>
}